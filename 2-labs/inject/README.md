# 故障注入说明（Staging）

> 说明：具体注入命令请按场景维护在 `labs/inject/` 中（脚本或 md 均可）。  
> 原则：可控、可恢复、可重复；注入后必须验证“确实产生预期现象”。

## 推荐做法
- 每个场景一个注入脚本：`SXX_*.sh`
- 每个脚本包含：
  - 注入动作
  - 注入成功验证（例如 curl 失败/指标异常）
  - 恢复动作（清理进程/恢复配置）

# 2）连接失败 / 连不上

> 来源：`故障排查肌肉记忆.md`（旧文件）。

**用户感知**

```bash
curl http://127.0.0.1:8080/
```

**故障注入**

```bash
sudo systemctl stop nginx
```

**运维起手（L1）**

```bash
systemctl status nginx
ps aux | grep nginx
ss -lnt
```

**恢复**

```bash
sudo systemctl start nginx
```

---
